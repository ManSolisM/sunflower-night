# 🌻 Girasoles de Noche

Animación Canvas con cielo nocturno, estrellas fugaces y girasoles creciendo desde el centro.

## 🚀 Uso local

```bash
npm install
npm start
```

## 🌐 Publicar en GitHub Pages

```bash
# 1. Sube a GitHub
git init && git add . && git commit -m "🌻 init"
git remote add origin https://github.com/TU_USUARIO/sunflower-night.git
git push -u origin main

# 2. Edita package.json: cambia "homepage" por:
# "https://TU_USUARIO.github.io/sunflower-night"

# 3. Despliega
npm run deploy
```

Tu página estará en `https://TU_USUARIO.github.io/sunflower-night` en ~1 minuto.
